{
  "name": "ltx",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "format": "turbo format --continue --",
    "format:fix": "turbo format --continue -- --write",
    "lint": "turbo lint --continue --",
    "lint:fix": "turbo lint --continue -- --write --unsafe",
    "clean": "turbo clean"
  },
  "dependencies": {
    "next": "^14.2.14",
    "turbo": "^2.4.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.3",
    "postcss": "^8.4.47"
  },
  "packageManager": "pnpm@10.4.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@biomejs/biome",
      "@prisma/client",
      "@prisma/engines",
      "@vercel/speed-insights",
      "esbuild",
      "prisma",
      "sharp"
    ]
  }
}
